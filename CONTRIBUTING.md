# 维护与贡献

先读 ARCHITECTURE.md，确认修改的是公开档案、模型流程、工具还是评测。
保留已有本地修改，开发用独立分支。公开包必须在没有开发者 .local 的情况下可以开始静态使用。

## 文档与入口

- 能力和安装变化同步 README、SKILL 和相关 references，避免多处复制长操作流程。
- 命令以实际 --help 为准，从干净环境验证；占位路径和合集键应明确标出。
- 历史报告写明时间、输入来源、验证范围；不能把开发机私有资产写成下载附带内容。
- 使用相对文档链接；公开文档不依赖维护者用户名、私人路径或原聊天上下文。

## 来源与研究规则

新增结论注明真实来源、适用范式、日期、限定条件和核验状态。
历史蒸馏、综合推断与官方政策分开；不把发表案例或模拟导师意见当录用标准。
改变审计项需同步稳定 ID、出处、相关流程和回归案例；不要仅为套住现有用例修改期望值。
不得提交无权公开的全文、稿件、评分者身份或凭据。私有卡片和数据放在 .local。

## 验证

在根目录运行：

```bash
python3 -m unittest discover -s tests -v
```

再按 examples/README.md 对相关行为做走查，核对 Markdown 链接、CLI 示例和 SKILL frontmatter。
若宿主提供 skill 校验器可额外运行，但它不是本仓库自带依赖。
软件测试通过只证明对应工具行为，不证明追问或创新评分更准确。
模型运行记录版本、实际模型、输入与上下文；明确开发集和留出集，区分模型评审与教师评分。

提交说明应包括问题、改后行为、验证和剩余限制。提交前检查 git diff 与 git status，
确保 .local、全文和凭据未被加入。创建本地修改或提交不等于已经发布 GitHub，报告时区分。
