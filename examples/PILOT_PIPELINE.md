# 研究推进主线：候选A试跑

日期：2026-09-10。维护前基线41项自动测试；本轮完成46项，全部通过。
本记录区分工具验证与宿主模型实际文献辅助走查。本轮没有新增独立模型盲测或教师评价。

## 输入及边界

输入为外部候选A：GenAI介入专业服务组织的练手任务，是否影响初级人员判断力形成。
源文件将其标为AI生成演练，因此按synthetic保存，不视为用户真实访谈或组织准入。
情境约束为12个月、单机构、不能实验/随机权限、无绩效原始数据；日志仅有口头承诺。
源文件、全文、检索记录和完整诊断均留在被忽略的 `.local/pilot-a/`，公开文件不含论文全文。

## 实际行为与修正

1. 固定知识快照 `b5321bd37c1c7a3f327d` 执行3组FTS搜索，每组8个同篇去重命中。
   原日志误取不存在的year字段，已用实际date字段并显式固定快照重跑；date仍只是索引元数据。
2. 从4篇种子扩展到6篇，读取相关方法/正文/讨论/局限，核对关键页图，未宣称完整系统综述。
3. 16564的Table 4已经提出know-how侵蚀问题，但它是未来问题，不是已验证结果。
   16773直接讨论AI交互中深浅反思，出版年是2023，不能从DOI中的2022推断刊期。
   因此把贡献候选转向跨任务学习机会、带教纠正与后续独立运用，仍未确认领域新颖。
4. 推荐过程路线，日志可选；事件追踪是新增待确认依赖。问卷只作有条件备选，不能凭两波
   宣称完整因果机制。保留培养安排路线作为退路，明确阻断不可执行的随机绩效设计。
5. 实际保存项目 `pilot-a-synthetic` revision 1及2，保留旧判断、触发证据、用户尚未选路线，
   并执行project-export。未假造研究数据或资源改善，跟踪的是本轮文献引发的判断改变。

## 软件验证

- `python3 -m unittest discover -s tests -v`：46项通过（本轮使用本地venv Python）。
- skill-creator的 `quick_validate.py .`：Skill is valid。
- `git diff --check`：通过。
- 实际 `plan-check .local/projects/pilot-a-synthetic.json`：process、survey_association、
  training_reframing为conditional；causal_performance为blocked。
- 新增5项测试：口头承诺/许可不全、可选日志失效、禁用方法/超时、无效依赖与数值，
  以及计划随保存/历史/分支/导出保留、失败保存不改变当前修订。

实现：SKILL入口 → research-pipeline参考 → 可选research_plan → research_planning检查。
检查器只按显式方法标签与资源状态判断依赖，不能核验资源真实性、发现全部暗含依赖，
也不能判断测量充分性、因果有效性、创新或录用。

## 尚未验证

本轮是单个合成项目的实际文献辅助开发走查，不能证明多课题泛化或优于普通助手。
未完成2024–2026年跨期刊GenAI检索，未收到真实事件证据，未新增独立盲评。
后续用真实脱敏事件及独立评审检查：最近邻漏检、贡献夸大、过程证据可执行性和判断校准。
