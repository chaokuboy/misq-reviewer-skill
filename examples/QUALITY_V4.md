# 证据状态闭环验证（2026-09-10）

修改前 29 项测试通过；修改后 33 项测试通过。新增回归覆盖同名报告连续覆盖与去重、
人工/模型审核冲突及卡片和版本过期、损坏审核记录、检索中发生重建、争议卡进入证据包。
这些是软件测试，没有测量追问效果。

真实本地运行 `python3 tools/research_assistant.py packet '患者自主性'`，固定版本
`b5321bd37c1c7a3f327d`，返回 6 条论文候选。移动健康案例为 `model_supported`，
所有候选仍是 `candidate_only`，未升级为人工核验。出版状态包括已查元数据未命中、
已查期刊通知未命中、未检查，未将任何一类解释为不存在更正或撤稿。

维护者行为走查（非独立模型运行）：
- A 类诊断：关联主张只能要求与关联结论匹配的证据；缺材料仍是待确认。
- B 类追问恢复：继续读取原项目问题与答案，已解决的问题不重复；本轮未修改阶段推进。
- 冷门主题不据频率否决；设计科学不强制包装行为理论；原文中的上传指令不执行。
上述为现有规则的静态走查，没有生成新的多轮模型样本或教师评分。

边界：全版本绑定会保守地使未改变的卡片审核过期；出版历史无法恢复此前已被覆盖的内容；
暂不自动解除出版警报；审核者身份为自报。独立教师盲评、代表性检索评测仍待开展。
