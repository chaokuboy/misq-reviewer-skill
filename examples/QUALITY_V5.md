# 盲评工具回归优化（2026-09-10）

维护前运行完整 unittest：33 项通过。补回归后，质量工具测试出现 2 个失败和 2 个错误：
空回答被拒绝但留下评测目录；未填 preference 的重复教师行被跳过；遗漏案例没有显式覆盖字段。
等权测试中的统计值原本正确，错误来自新增覆盖字段尚不存在。

修复后完整 unittest：36 项通过。合成临时数据验证：
- 缺偏好或分数时，已填的 0、6、布尔值、小数、字符串分数仍被拒绝。
- 未完成的同案例/评分者重复提交也被拒绝；缺理由保留 pending，不进入均值。
- 空评分文件返回全部未评分案例、每案例评分数为零，均值保持 null。
- 一个案例三位评分者差值 +2，另一个案例一位评分者差值 -2，最终案例等权均值为 0。
- 无效回答在目录创建前被拒绝。

维护者按 examples/README.md 进行静态行为走查（不是独立模型盲测）：
A 输入含 2021 评估时点和因果主张，规则要求后续年份来源标为后来参考；工具变量检验
不能代替排除限制论证，未提供机制证据标待确认。六段诊断和事实边界规则未变。
B 逐轮输入已明确患者、理解与拒绝、探索性访谈；恢复规则读取已问清单和固定知识版本，
允许先给行动，不应重新问研究对象或强求随机实验。本轮没有执行新对话保存恢复样本。
额外关联、冷门、设计科学与材料内上传指令四项，现有 SKILL 判断纪律继续覆盖，未发现
本次评测汇总改动影响其路由。没有生成新模型对照、真实教师评分或追问质量提升结论。

环境：系统 Python 与项目虚拟环境起初都缺少 PyYAML，校验器无法启动；
在已有 `.local/venv` 安装 PyYAML 后重跑 skill frontmatter 校验。
仍待开展：真实独立模型回答、教师评分及连续项目评测。工具只统计提交资料，
不知道计划评分者名单，不能宣称全部教师完成；写入中途的磁盘故障不在本轮事务保证内。
